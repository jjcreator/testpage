import { createMuiTheme } from '@material-ui/core/styles';


const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#757575",
    },
    secondary: {
      main: "#2196F3",
    },
    textSecondary: {
      main: "#757575"
    },
    textPrimary: {
      main: "#000"
    }
  },
});

export default theme;